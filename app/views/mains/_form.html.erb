<%= form_for(main) do |f| %>
  <% if main.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(main.errors.count, "error") %> prohibited this main from being saved:</h2>

      <ul>
      <% main.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :view %>
    <%= f.text_area :view %>
  </div>

  <div class="field">
    <%= f.label :text_entry %>
    <%= f.text_area :text_entry %>
  </div>

  <div class="field">
    <%= f.label :patient_id %>
    <%= f.text_field :patient_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
